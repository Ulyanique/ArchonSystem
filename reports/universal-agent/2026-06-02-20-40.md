# Universal-Agent Report
**Date:** 2026-06-02 20:40
**Role:** builder + tester

## Задача
[высокий] Исправить удаление технологий и артефактов (п. 0.1.1 Stabilization). Заменить прямое удаление на `useMutation` с инвалидацией в `TechnologiesPage.tsx`.

## Артефакты
- `frontend/src/pages/TechnologiesPage.tsx`
- `backend/tests/test_services.py` (исправление падающего теста)
- `backlog/universal-agent.md` (обновление статуса задачи)

## Pre-flight
- Проанализирован `TechnologiesPage.tsx`. Замечено, что удаление реализовано через `onClick` с вызовом функции API без оборачивания в `useMutation`.
- Изучена `backend/tests/test_services.py`, так как во время тестирования он падал из-за отсутствия mock-метода `get_or_create_collection`.

## DoD-check
- [x] Заменить прямое удаление на `useMutation`
- [x] Инвалидировать `['techs', universeId]` и `['artifacts', universeId]` после удаления.
- [x] После удаления элемента список в UI обновляется без перезагрузки (проверено в браузере с Playwright).

## Тесты
- `npm test` в директории frontend — успешно (15/15).
- `pytest` в директории backend — успешно (23/23).
- UI E2E с использованием Playwright (добавление и удаление технологии) — визуально подтверждено. Уведомление об удалении выводится.

## Предложения по улучшению
- В проекте все еще остаются места, где могут быть прямые вызовы API (без react-query) или вызовы `window.confirm`. Необходимо продолжить заменять `confirm()` на кастомный модал согласно п. 0.3.2 Stabilization.
- Желательно вынести `queryClient` настройки в отдельный файл/модуль для более строгой типизации ключей кэша.
