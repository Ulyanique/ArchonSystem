import UniversesPageModern from './UniversesPageModern';

/** Главная страница списка вселенных — современный вид. */
export default function UniversesPage() {
  return (
    <div className="relative overflow-x-hidden">
      <UniversesPageModern />
    </div>
  );
}
