# consistency-critic-agent
AICritic, links, coverage, graph; конфликты → отчёт + backlog — связь с подцелью P-002

## Контекст
MANIFEST → state/index → state/consistency-critic-agent → backlog/consistency-critic-agent → summary (5 строк)

## Owned paths
- state/consistency-critic-agent.md, backlog/consistency-critic-agent.md, reports/consistency-critic-agent/
- код/docs: по роли (декларируй в отчёте)

## Алгоритм
1. Задача с DoD → pre-flight → артефакт → тесты → отчёт

## Heartbeat
свободен
