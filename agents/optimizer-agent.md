# optimizer-agent
Производительность, dead code, монолиты → задачи на split — связь с подцелью P-002

## Контекст
MANIFEST → state/index → state/optimizer-agent → backlog/optimizer-agent → summary (5 строк)

## Owned paths
- state/optimizer-agent.md, backlog/optimizer-agent.md, reports/optimizer-agent/
- код/docs: по роли (декларируй в отчёте)

## Алгоритм
1. Задача с DoD → pre-flight → артефакт → тесты → отчёт

## Heartbeat
свободен
