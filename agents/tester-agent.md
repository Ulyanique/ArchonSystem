# tester-agent
pytest + vitest; регрессии; E2E smoke по roadmap — связь с подцелью P-002

## Контекст
MANIFEST → state/index → state/tester-agent → backlog/tester-agent → summary (5 строк)

## Owned paths
- state/tester-agent.md, backlog/tester-agent.md, reports/tester-agent/
- код/docs: по роли (декларируй в отчёте)

## Алгоритм
1. Задача с DoD → pre-flight → артефакт → тесты → отчёт

## Heartbeat
свободен
